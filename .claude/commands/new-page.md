Create a new page for: $ARGUMENTS

1. Create the page component in `apps/frontend/src/pages/`
2. Follow existing page patterns (use relevant contexts, PageHeader, etc.)
3. Add the route in the router (find the router setup in the app)
4. Add navigation entry in VerticalSidebar.tsx and mobile navigation
5. Create any needed components in `apps/frontend/src/components/`
6. Add service functions in `apps/frontend/src/lib/` if needed
7. Run type-check: `cd apps/frontend && npm run type-check`
