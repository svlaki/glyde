Run the quality pipeline for the Glyde project:

1. Run `cd apps/agents && npm run quality` and report results
2. Run `cd apps/frontend && npm run quality` and report results
3. Summarize: total errors, warnings, test failures
4. If anything fails, suggest specific fixes
